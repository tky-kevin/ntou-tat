# NTOU TAT Environment Variables

# Comma-separated list of proxy worker URLs
# The application will try these in order as fallbacks if one fails.
VITE_NTOU_PROXY_URLS="https://ntou-proxy.tky-kevintang.workers.dev/"
